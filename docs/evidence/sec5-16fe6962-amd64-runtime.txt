Section 5 exact target-tail runtime artifact evidence

Recorded through: 2026-09-13T10:44:10Z
Repository: samdefmacro/ethereum-lisp
Branch: sec5-public-bootstrap-sync
Revision: 16fe6962837be612281e2659aa91ad100f85d9c1
Functional revision: 40be2940cef4475ff55c3345286972aced6d23ce

Scope

This record preserves an exact linux/amd64 runtime image and export containing
the SNAP target-tail completion guard. Revision 16fe6962 differs from functional
revision 40be2940 only by its reviewed Section 5 evidence and plan update. This is
local artifact evidence, not upload, deployment, Hive, or Hoodi completion
evidence.

Build boundary

After the canonical checkout advanced, `cl-workbench doctor --strict` passed
again. The checkout was clean, and local HEAD plus
`origin/sec5-public-bootstrap-sync` both resolved to the exact revision before
the build. The documented project brokers built, exported, and smoked the
artifact:

  RUNTIME_PLATFORM=linux/amd64 scripts/dev.sh runtime-build \
    ethereum-lisp-runtime:sec5-16fe6962-amd64
  scripts/dev.sh runtime-export \
    ethereum-lisp-runtime:sec5-16fe6962-amd64 \
    /private/tmp/ethereum-lisp-runtime-sec5-16fe6962-amd64.tar
  cl-workbench validation run runtime-smoke \
    ethereum-lisp-runtime:sec5-16fe6962-amd64

The guarded sequence started at 2026-09-13T10:42:08Z, finished at
2026-09-13T10:44:10Z, and reasserted the clean exact revision afterward.

Artifact identity

- image: `ethereum-lisp-runtime:sec5-16fe6962-amd64`;
- image ID:
  `sha256:0176ca649100875c843cfdc43b4abd51193bbb2930311ec87bfb4485fa0a197d`;
- platform: `linux/amd64`;
- revision label: `16fe6962837be612281e2659aa91ad100f85d9c1`;
- configured user: `ethereum:ethereum`;
- image size: 71,990,158 bytes;
- archive: `/private/tmp/ethereum-lisp-runtime-sec5-16fe6962-amd64.tar`;
- archive size: 72,015,360 bytes;
- archive mode: 0600;
- archive SHA-256:
  `99fc93388c1fe3df7f9c5358217d56858af6cf73113488d4e49b86d2767dc8ab`.

Runtime verification

The runtime-smoke gate exited zero. It proved:

- packaged `--hoodi` genesis construction;
- public `eth_chainId=0x539`;
- `web3_clientVersion` reports `0x16fe6962`;
- unauthenticated Engine access returns HTTP 401;
- authenticated `engine_exchangeCapabilities` responds;
- `engine_getClientVersionV1` identifies the full exact revision;
- Engine-port `eth_syncing` responds.

The retained build/export/smoke log is
`/Users/sen/.hermes/runtime/ethereum-lisp-sec5-supervisor/cycle1000-16fe6962-runtime.log`,
SHA-256
`a965919fa5d9ec33828307e5adbabfcf764ef14bb6ec449ddefeac918b5a20da`.

Live boundary

No remote EL, CL, port, network, image, container, resource setting, or datadir
was mutated. At 2026-09-13T10:20:58Z, the older exact-0c6b51bf Hoodi process had
been running for roughly 42 hours with no restart or OOM, but still reported
`eth_blockNumber=0x0`, `eth_syncing.currentBlock=0x0`, zero target-completion
events, and an expanding healer frontier. The local runtime artifact has not been
uploaded or deployed.

The remote `/data` filesystem then had 51,835,883,520 bytes available, below the
85,899,345,920-byte fresh-validation precondition by 34,063,462,400 bytes
(31.724071502685547 GiB). A fresh deployment requires additive capacity and a
new independently reviewed authorization request. Existing containers,
datadirs, artifacts, and evidence remain preserved.
