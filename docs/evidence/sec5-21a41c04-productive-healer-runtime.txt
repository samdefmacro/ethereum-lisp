Section 5 productive-healer runtime artifact evidence

Recorded through: 2026-09-12T06:19:07+08:00
Revision: 21a41c04d0088a002a919070d801c832de828f59
Functional revision: 9f697ec645bf1d9e9ff544bfce8c37018aaa65c8
Branch: sec5-public-bootstrap-sync

Scope

This evidence records the exact linux/amd64 runtime artifact containing the
productive local-healer expansion repair. Revision 21a41c04 differs from the
functional revision only by the Section 5 plan and live-evidence update. It is
an accepted local artifact, not a deployment or live-completion claim.

Build boundary

`cl-workbench doctor --strict` passed before application tooling. The canonical
checkout was clean at revision 21a41c04d0088a002a919070d801c832de828f59. The
runtime was built and exported through the documented project broker:

  RUNTIME_PLATFORM=linux/amd64 scripts/dev.sh runtime-build \
    ethereum-lisp-runtime:sec5-21a41c04-amd64
  scripts/dev.sh runtime-export \
    ethereum-lisp-runtime:sec5-21a41c04-amd64 \
    /private/tmp/ethereum-lisp-runtime-sec5-21a41c04-amd64.tar

Artifact identity

- local image: ethereum-lisp-runtime:sec5-21a41c04-amd64;
- image ID: sha256:0e52fcb439e250b3eed1504bcdb0d04514f44cfbe4d8e7d87461c10348e38a88;
- platform: linux/amd64;
- revision label: 21a41c04d0088a002a919070d801c832de828f59;
- configured user: ethereum:ethereum;
- image size: 71,959,706 bytes;
- archive: /private/tmp/ethereum-lisp-runtime-sec5-21a41c04-amd64.tar;
- archive size: 71,984,640 bytes;
- archive SHA-256:
  6222470a807314fb4a443e2f87b146622d2e552abbd940688daa40cc72530d04;
- archive mode at verification: 0600.

Runtime verification

`cl-workbench validation run runtime-smoke
ethereum-lisp-runtime:sec5-21a41c04-amd64` exited zero. The broker verified the
packaged Hoodi genesis allocation and then started the amd64 image under
emulation with temporary public and Engine endpoints. Seven checks passed:

- `--hoodi` built the packaged genesis;
- public `eth_chainId` returned `0x539`;
- `web3_clientVersion` identified `0x21a41c04`;
- unauthenticated `engine_exchangeCapabilities` returned HTTP 401;
- authenticated `engine_exchangeCapabilities` answered;
- `engine_getClientVersionV1` mapped the full revision to `0x21a41c04`;
- Engine-port `eth_syncing` answered.

Live boundary

The exact-0c6b51bf Hoodi validation container and datadir were not changed,
restarted, or replaced while this artifact was built. The new runtime has not
been uploaded or deployed. It therefore does not prove the repair against the
live moving-pivot condition. A future live action still requires independent
authorization, a fresh unique datadir, preserved rollback evidence, and enough
remote filesystem capacity; the current `/data` filesystem is already at 96%
utilization and cannot safely absorb another approximately 56-GiB fresh sync.
