Section 5 Hoodi run at 8e95b990: healing converges again, the node follows the
head with VALID payloads, and two post-heal defects surface

Status

Fresh-datadir run started 2026-09-23T06:21:58Z (container hoodi-el-sec5-8e95b990,
datadir-8e95b990, 12 GiB, replacing the stopped aac5f762 container). Not the
completion record: `complete` fails on one check (below). The run is still up
at the time of writing, executing the CL's payloads at the head.

Range phase

06:22Z to about 09:10Z. The first three minutes made no progress because
Lighthouse had fallen 75 slots behind while the EL was down and every pivot it
authorized was already pruned by peers (244 pivot_unavailable); it caught up
by itself. Then 0 import_failed, 0 storage_failed, 71 dependency_failed (all
"Snap peer does not have the requested storage-range state", i.e. pivot expiry
handled by rebase). With the level-per-multi-get closure walk (4a7e46a7),
peer.snap.storage_closure reported 213 closed / 6 already-closed attempts in
one 12-minute window at 2.7-6 s each (14-16k nodes, 22-34 multi-gets, 7-8
levels), and page storageMs was 0.8-41 s against 90-142 s on aac5f762 and
0.4 s on 75b0b7a7. The remaining cost is disk-bound multi-gets on the page
thread; not blocking.

Healer

  09:11:39Z  start at pivot 3679588 (one fetched node)
  09:12:09Z  processed 27,597 fetched 27,598 reused 60,876 requests 2,008
  09:12:15Z  processed 39,996 fetched 39,985 reused 70,847 skipped 301,778
             frontier 0 knownIncompleteNodes 0 completed=T
  09:15:54Z  peer.snap.target_completed pivot 3679588 target 3679652

36 seconds from first fetch to completion, the second live convergence in a
row (aac5f762: 34 s at pivot 3678032). Read width stayed wide (localReadWorks
per batch about 30).

Exit at 09:17:32Z (exit 1, OOMKilled=false)

After target_completed the node installed the pivot and followed the head
through the forward path (dozens of peer.sync.gap_filled "1 block" events
09:15:55-09:17:18). At 09:17:19 the HTTP listeners finished and the process
printed, as its last line,

  couldn't read from #<SB-SYS:FD-STREAM for "socket 172.18.0.3:30303, peer: 3.237.241.129:58278" {10026DA433}>:
    Connection reset by peer

172.18.0.3:30303 is this node's own listener, so an inbound session's read
error reached the sync coordinator's fatal handler (the set it absorbs is
devnet-peer-sync-invalid, eth-sync-multi-peer-error and
snap-sync-sources-exhausted; a raw stream error is not in it). A peer hanging
up must be that peer's failure, not the node's. Fixed separately
(sec5-forward-sync-peer-errors.txt when it lands).

Restart recovery at 09:19:36Z

`scripts/hoodi-live-gate.sh restart` (the broker accepts a stopped container)
brought the node back on the same datadir: eth_syncing immediately reported
currentBlock 0x382564 (the installed pivot 3679588), the healer confirmed the
retained state in one step, and the tail executed forward at about 3 s per
block: pivot_rebased/target_completed pairs 3679623, 3679625, 3679635,
3679642, 3679645, 3679646, 3679647 between 09:20 and 09:22.

Head following

From 09:24Z every Engine call from Lighthouse returned VALID:
engine_newPayloadV4 x 31 and engine_forkchoiceUpdatedV3 x 2 in the ten
minutes to 09:34 (handlerMs 1.8-29 s). eth_blockNumber at 09:38Z answered
0x3825ed (3679725), past the last snap target. The node is executing the
canonical head through the CL-authorized chain.

What `complete` says

  completion-eth-syncing=not-false   exit 1

eth_syncing kept answering {"startingBlock":"0x382564","currentBlock":"0x382564",
"highestBlock":"0x3825df"} for 20+ minutes while eth_blockNumber moved past
0x3825df. The snapshot is refreshed by a thread that needs the node store
guard; with the guard busy for 5-25 s at a time (execution, batch import,
Engine handlers on this revision, which predates the Engine-priority guard of
c831c9a6) the refresh is missed and the previous snapshot is answered. Public
eth_blockNumber calls also waited 10-30 s behind the same guard. Fixed
separately (sec5-eth-syncing-live-head.txt when it lands). Every other
acceptance signal of the plan is present in this run: target_completed,
healer completed=true with an empty frontier, the canonical head executed
through the CL target.

Remote housekeeping during this run

/data reached 95% after the aac5f762 forensics (two 57 GB chaindata copies);
the copies, the verify container, datadir-cceee42f, datadir-3305307d,
datadir-aac5f762 and datadir-75b0b7a7 were removed under the user's
authorization; /data is at 54% with only datadir-8e95b990, the geth baseline
and the Lighthouse datadir left.
