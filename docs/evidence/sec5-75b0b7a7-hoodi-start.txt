Section 5 fresh-datadir Hoodi run started at 75b0b7a7

Status

Run start record only. Not completion. The acceptance criteria in
docs/gap-analysis/public-testnet-readiness-plan.md (one continuous
fresh-datadir run recording peer.snap.target_completed, healer completed=true
with an empty frontier, eth_syncing=false, canonical EL head executed through
the CL-authorized target) are unmet at the time of writing and are checked
later with the read-only `complete` action.

Revision

75b0b7a7554810274c3416da327e8aea7ed7fcb4 (branch sec5-public-bootstrap-sync).
Runtime image ethereum-lisp-runtime:sec5-75b0b7a7-amd64, remote image id
sha256:3b12570dbfeac7af7139ec590e3c95adac6d337e4a116ff28a52e78fc7e936be,
artifact sha256 9de615d24fe301978bd4c1fe6045395e7186ff509cc0c95b9f6066be177fb5c9
(72,082,944 bytes). Exact-revision gates are in
sec5-75b0b7a7-amd64-runtime.txt (cold-all, runtime-smoke seven of seven).

What this revision changes relative to the 3305307d run it replaces

The 3305307d run stalled for four days in the healer with the signature every
earlier run showed (sec5-3305307d-hoodi-stall.txt): promotedSubtrees 0,
knownIncompleteNodes tracking processedNodes, netDrainRate negative,
etaStatus dynamic-expansion. Its last read-only sample, at
2026-09-22T17:03Z, showed processedNodes 659,456, frontierWorks 695,568,
knownIncompleteNodes 583,403, skippedSubtrees 3,508,392 and
eth_blockNumber 0x0 after 4 days.

75b0b7a7 is the account-side closure change that trace asked for, plus the
work around it, in six merges (listed in sec5-75b0b7a7-amd64-runtime.txt):

- closure epoch seven: the range phase writes an account trie node only when
  its whole subtree and every code hash and storage root it names are
  durable, and the healer skips a present, unmarked account node without
  descending (docs/snap-account-closure.md; the marked-before-skip order is
  what makes the healer's own top-down flush safe). Locally, the mid-range
  rebase reproduction decodes 21 nodes instead of 322 and the page-density
  fixture expands 64 account nodes instead of 257 after a rebase, all of
  them fetched. On this run the metric to watch is knownIncompleteNodes
  staying far below processedNodes and frontierWorks draining.
- the snap/1 server persists nothing it serves;
- the healer's local read batch stays 130 wide above the live frontier bound
  (the 3305307d run was reading one node per batch); peer.snap.heal_progress
  now carries localReadBatches and localReadWorks so the width is visible;
- the node closes its RocksDB handles and releases the background pools
  before exit(3), so the exit-time memory fault's teardown is not reached.

Authorization

The user authorized, in session on 2026-09-23, any operation on the Hoodi
host needed to complete this blocker, disk cleanup included, without further
requests. The cleanup performed before the start is recorded in
sec5-75b0b7a7-amd64-runtime.txt.

Broker invocation

All actions ran from a worktree detached at exactly 75b0b7a7
(.claude/worktrees/gate-75b0b7a7) with HOODI_GATE_HOST=
test-ethereum-sophon2-symbiosis and HOODI_GATE_MEMORY_BYTES=12884901888.

  scripts/hoodi-live-gate.sh inspect   exit 0   2026-09-22T19:42:51Z
  scripts/hoodi-live-gate.sh upload    exit 0   2026-09-22T19:42:54Z
                                       uploaded-artifact sha256 9de615d2...
                                       (verified by the broker; no manual
                                       rename needed this time)
  scripts/hoodi-live-gate.sh load      exit 0   2026-09-22T19:44:13Z
  scripts/hoodi-live-gate.sh start     exit 0   2026-09-22T19:44:18Z
                                       with HOODI_GATE_OLD_CONTAINER=
                                       hoodi-el-sec5-3305307d and
                                       HOODI_GATE_OLD_REVISION=3305307d...

start output (verbatim tail):

  ==> cutting the existing Lighthouse alias over to the exact-revision EL
  container=/hoodi-el-sec5-75b0b7a7 running=true started=2026-09-22T19:44:52.970850459Z image=sha256:3b12570d... user=1000:1000 read-only=true memory=12884901888 memory-swap=12884901888 caps=["ALL"] security-options=2 networks=2
  fresh-datadir=/data/hoodi-sec5-20260814/datadir-75b0b7a7 uid=1000 gid=1000

The replaced container

hoodi-el-sec5-3305307d had run since 2026-09-18T09:17:56Z without
converging. The broker stopped it with docker stop --time 30; it exited 137
at 2026-09-22T19:44:52.049Z, OOMKilled=false, i.e. SIGKILL after the grace
period. Its last log line is "Devnet shutdown requested; closing RPC
listeners." and its RocksDB LOG records no shutdown, as expected for a
revision that predates both the keep-alive stop fix (6f8b80eb) and the
store close (6a6dbbb8). This is the fifth observation of the old shutdown
shape and the last one that can occur: 75b0b7a7 carries both fixes. Its
datadir is preserved for now and cannot be reused (epoch six).

First read-only samples

status at 2026-09-22T19:45:30Z (38 s after start):

  runtime=cpu=277.93% memory=1.403GiB / 12GiB blockIo=4.62MB / 1.24GB pids=121
  datadir-bytes=468294329
  eth_blockNumber 0x0
  eth_syncing {"startingBlock":"0x0","currentBlock":"0x0","highestBlock":"0x381770"}
  net_peerCount 0x7

logs at 2026-09-22T19:45:30Z, selected lines:

  el-event=peer.snap.progress count=44
  el-event=peer.snap.heal_progress count=0
  el-event=peer.snap.pivot_rebased count=0
  el-event=peer.snap.pivot_unavailable count=0
  el-event=peer.snap.storage_failed count=0
  el-event=peer.snap.import_failed count=0
  el-event=peer.snap.target_completed count=0
  el-negotiated=eth72-snap1 count=5
  el-storage-profile=pivot value=3675949
  el-storage-profile=slotsPerSecond value=31241
  el-runtime-fault-count=0

At 19:46:07Z the datadir held 1,134,240,127 bytes after 91 progress events.
Range download is under way with no failure class recorded. Healing has not
started.

What is still owed for this revision

- The healer outcome, sampled with status/logs every fifteen minutes, and
  the acceptance criteria above checked with `complete`.
- Hive rpc-compat, Engine/auth and devp2p at 75b0b7a7, remote-only, after
  the live run no longer needs the host's memory; the prepared commands in
  the sec5-3305307d-hive-*.txt records apply with the revision substituted.
- The seven-day shadow comparison and the fourteen-day validator soak.
