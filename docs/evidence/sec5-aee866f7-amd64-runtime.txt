Section 5 exact linux/amd64 runtime artifact evidence

Recorded at: 2026-09-23T17:40:00Z
Branch: sec5-public-bootstrap-sync
Revision: aee866f7 (git rev-parse aee866f7 for the full id)
Image: ethereum-lisp-runtime:sec5-aee866f7-amd64
Local image ID:
  sha256:16cfd45a8d0e6e4f5aab7333f26696bae930834d76fbb99c6069e4a2df865d54
Platform: linux/amd64
Local image size: 72193212 bytes

Revision under test

aee866f7 is the second release candidate of 2026-09-23. Relative to d203fee6
(sec5-d203fee6-amd64-runtime.txt, whose fresh-datadir run passed `complete`,
sec5-d203fee6-hoodi-complete.txt) it adds the afternoon's parallel work,
each merge verified with targeted tests at merge time:

  cab507ee  Section 10 operations: SIGKILL/SIGTERM recovery tests, lock-free
            metrics, docs/runbook.md (sec5-ops-recovery.txt)
  e313f198  bounded payload building: one execution per candidate,
            preemptible improvement passes, bounded getPayload
            (sec5-payload-building.txt)
  e0b76c48  newPayload read savings and np* phase telemetry
            (sec5-newpayload-latency.txt)
  747ca812  Hive Engine regressions: the inbound gossip gate reads the
            published sync view (sec5-engine-regressions-d203fee6.txt)
  34f2e8ae  RPC hardening: immutable read view off the guard, response work
            budgets, WebSocket enforcement (sec5-rpc-hardening.txt)
  aee866f7  restart-behind catch-up: a synced head forward-syncs up to 8192
            blocks, known blocks are never re-buffered
            (sec5-restart-behind-resnap.txt)
plus the d203fee6 gate records (complete, EEST, Hive rpc-compat / engine /
devp2p) and one gate-script fix (9f8f336e, `complete` reads the whole log).

Exact-revision gates

cl-workbench validation run cold-all on aee866f7, exit 0, no `not ok` line,
no compile warning under /workspace/src (log kept in the session scratchpad
as final5-cold-all.log):

  1450 tests passed, 3 skipped.   (unit)
  620 tests passed, 9 skipped.    (integration)
  40 tests passed.                (e2e)
  36 tests passed.                (e2e)

Against the d203fee6 record (1436 / 602 / 33 / 36): +14 unit, +18
integration, +7 e2e, the tests the merges above added. An integration
cold-all at 34f2e8ae (before the last merge) had already passed with
1450 / 616 / 40 / 36.

Build and revision fence

The checkout was on the branch at aee866f7 and clean before the build, and
remained clean through build, export and smoke:

  RUNTIME_PLATFORM=linux/amd64 scripts/dev.sh runtime-build \
    ethereum-lisp-runtime:sec5-aee866f7-amd64
  scripts/dev.sh runtime-export ethereum-lisp-runtime:sec5-aee866f7-amd64 \
    /private/tmp/ethereum-lisp-runtime-sec5-aee866f7-amd64.tar

Exported artifacts

  /private/tmp/ethereum-lisp-runtime-sec5-aee866f7-amd64.tar
  72218112 bytes
  sha256 ab52095f1204474e21e9c7a4e3eca959374a00bf32132dbea035609d5c4874c5
  /private/tmp/ethereum-lisp-source-aee866f7.tar (git archive --format=tar aee866f7)
  sha256 2f3f6ebadba4d2339561bb5b686e5f395e56821ff1b9879bcc52793396c1496a

Runtime smoke

cl-workbench validation run runtime-smoke ethereum-lisp-runtime:sec5-aee866f7-amd64
passed, exit 0 (PASS: runtime image ethereum-lisp-runtime:sec5-aee866f7-amd64
serves public RPC and an authenticated Engine API).

Staging on the host

Live gate inspect / upload / load from the worktree detached at aee866f7,
all exit 0 (uploaded-artifact sha256 as above); Hive broker `upload` exit 0
(source and runtime tars under /data/hoodi-sec5-hive/staging). The live run
and the gate reruns are recorded separately.
