Section 5 exact amd64 artifacts for pinned Hive devp2p rerun

Recorded: 2026-09-12T06:29:06Z
Revision: 4097bbd40843f76fbc8c0c402e39f85cce6708e6
Branch: sec5-public-bootstrap-sync

Scope

This revision repairs the retained pinned devp2p SNAP failures by serving the
canonical empty bytecode, preserving GetTrieNodes response cardinality with
post-append byte-threshold semantics, and emitting slim AccountRange bodies
with geth-compatible byte accounting and the two-MiB response clamp. The
artifacts below are exact inputs for an independently authorized rerun of the
48-case pinned devp2p inventory.

Runtime image

Image tag:
  ethereum-lisp-runtime:sec5-4097bbd4-amd64
Local Docker Desktop image ID:
  sha256:359cbe40ddabab3eaabfda71dd80e10b8d6fea40934ce266fcb430a408a15dad
Exported archive config / loaded image ID:
  sha256:fbf32503f8b4247cccbb83b188d03a8b1f78de524c345b1a2ed59a29b09a429b
Platform:
  linux/amd64
Configured user:
  ethereum:ethereum
OCI revision label:
  4097bbd40843f76fbc8c0c402e39f85cce6708e6
Export path:
  /private/tmp/ethereum-lisp-runtime-sec5-4097bbd4-amd64.tar
Export size:
  71994880 bytes
Export SHA-256:
  464e330346eebb4ba8ed6af59653bc141489ff087b15a1c54b914b31008beb79

Source archive

Archive path:
  /private/tmp/ethereum-lisp-source-4097bbd4.tar
Export size:
  29153280 bytes
Export SHA-256:
  475bdb73815821e89446646664cecf60c481b37021d5ae8f692fa275cb66b9cd
Archive inventory:
  878/878 tracked revision entries; 0 missing, 0 extra; 878/878 Git blob
  identities matched.

Operational correction

The first r34 launch script accidentally copied a 61-character transcription
of the source checksum into its fail-closed comparator. It therefore exited at
the source checksum check before archive extraction or runner creation. A
read-only follow-up verified that the preserved remote archive itself has the
64-character SHA-256 recorded above, `tar -tf` exits zero, the source directory
is still empty, and no r34 container exists. The preserved r34 evidence path was
not reused.

The fresh r35 setup matched all four archive hashes and loaded the runtime into
its nested Linux Docker daemon, then stopped before Hive because its comparator
used Docker Desktop's local containerd image identity
`sha256:359cbe40ddabab3eaabfda71dd80e10b8d6fea40934ce266fcb430a408a15dad`.
The portable archive's manifest names config blob
`sha256:fbf32503f8b4247cccbb83b188d03a8b1f78de524c345b1a2ed59a29b09a429b`,
and Linux `docker load` correctly exposes that config digest as the image ID.
Direct archive inspection and the retained r35 readback both prove the loaded
image is linux/amd64, non-root `ethereum:ethereum`, and labels revision
`4097bbd40843f76fbc8c0c402e39f85cce6708e6`. The runner exited
without OOM or restart; no Hive case executed, and the fresh r35 path remains
preserved. A successor request must use a new path/container and compare the
loaded image against the archive config digest above.

Verification

- `cl-workbench doctor --strict`: exit 0.
- Focused warm GetByteCodes, GetTrieNodes, AccountRange, malformed-wire,
  unavailable-root, persistence, and healer regressions: passed.
- `cl-workbench validation run cold-unit`: 1,356 passed, 3 skipped, exit 0.
- `cl-workbench validation run cold-integration --match SNAP-BYTECODE`:
  4 passed, exit 0.
- `cl-workbench validation run cold-integration --match
  SNAP-TRIE-NODE-SERVER`: 2 passed, exit 0.
- `cl-workbench validation run cold-integration --match SNAP-ACCOUNT-RANGE`:
  4 passed, exit 0.
- `cl-workbench validation run cold-integration --match SNAP`: 96 passed,
  exit 0.
- `RUNTIME_PLATFORM=linux/amd64 scripts/dev.sh runtime-build
  ethereum-lisp-runtime:sec5-4097bbd4-amd64`: exit 0.
- `cl-workbench validation run runtime-smoke
  ethereum-lisp-runtime:sec5-4097bbd4-amd64`: exit 0.
- Public `eth_chainId`: `0x539`.
- Public `web3_clientVersion`: `ethereum-lisp/0.1.0/CL/0x4097bbd4`.
- Unauthenticated Engine API request: HTTP 401.
- Authenticated `engine_exchangeCapabilities`: answered.
- `engine_getClientVersionV1` revision:
  `4097bbd40843f76fbc8c0c402e39f85cce6708e6`.
- `scripts/dev.sh hive-adapter-smoke
  ethereum-lisp-runtime:sec5-4097bbd4-amd64`: exit 0.
- Adapter refusals: 6/6, including `HIVE_DISCV5`.
- Bundled discovery genesis startup, uploaded fork-config preservation,
  Hive chain-ID mapping, production RocksDB assertion, routable enode address,
  and authenticated Engine capability checks: passed.

Boundary

These checks establish exact source/runtime identity and local amd64 startup,
adapter, unit, and SNAP integration behavior. They do not close the Hive
devp2p gate. A fresh independently authorized Linux run must load these
verified inputs, execute the full 48-case inventory under pinned Hive
`dde4f59d04ff0ff8b6585670b08cea1b6c8ab65c` and pinned geth
`101035a1049c7dc468bfe973478b579d9883d7b6`, and archive complete results.
The healthy Hoodi validation at runtime revision
`0c6b51bf6ea4852ddf2baf47147ce0ab24bbf4ae` was not changed.
