Section 5 exact linux/amd64 runtime artifact evidence

Recorded at: 2026-09-23T21:05:00Z
Branch: sec5-public-bootstrap-sync
Revision: 880319df (git rev-parse 880319df)
Image: ethereum-lisp-runtime:sec5-880319df-amd64
Local image ID:
  sha256:bfdbb742867db9bb01ec496fdc60e9256459eda1a5cc030858a9a47f2384428a
Platform: linux/amd64
Local image size: 72207534 bytes

Revision under test

880319df is the third release candidate of 2026-09-23. Relative to aee866f7
(sec5-aee866f7-amd64-runtime.txt; fresh-datadir run accepted at
sec5-aee866f7-hoodi-complete.txt) it adds one merge:

  880319df  Engine guard deferral and GC/guard attribution telemetry for
            newPayload and forkchoiceUpdated (sec5-newpayload-six-second-quantum.txt):
            ordinary guard callers stay off the mutex while an Engine request
            waits (bounded 2 s); handler logs carry GC ms/count, CPU ms, heap
            MB, peer wait and guard wait with the holders' labels; a
            node.store_guard.long_hold event for holds of 1 s or more.
plus the aee866f7 gate records (complete, EEST) and the aee866f7 runtime
record. src/ differs from aee866f7 only by that merge.

Exact-revision gates

cl-workbench validation run cold-all on 880319df, exit 0, no `not ok` line,
no compile warning under /workspace/src (log kept in the session scratchpad
as final7-cold-all.log):

  1454 tests passed, 3 skipped.   (unit)
  621 tests passed, 9 skipped.    (integration)
  40 tests passed.                (e2e)
  36 tests passed.                (e2e)

Against the aee866f7 record (1450 / 620 / 40 / 36): +4 unit, +1 integration,
the tests the merge added.

A first cold-all on this revision (final6-cold-all.log) exited 2 with one
e2e failure, PHASE-A-SMOKE-GATE-SCRIPT-CAN-INCLUDE-DEVNET-SUITE asserting
(= 0 STATUS): the smoke gate's devnet suite missed its deadline because the
cold lane was running at the same time as the emulated linux/amd64 runtime
build of this very image on the same control plane. The test passed alone
in the cold lane, and the full rerun above ran with nothing else local. The
cold lanes and the emulated build are not to be run concurrently.

Build and revision fence

The checkout was on the branch at 880319df and clean before the build, and
remained clean through build, export and smoke:

  RUNTIME_PLATFORM=linux/amd64 scripts/dev.sh runtime-build \
    ethereum-lisp-runtime:sec5-880319df-amd64
  scripts/dev.sh runtime-export ethereum-lisp-runtime:sec5-880319df-amd64 \
    /private/tmp/ethereum-lisp-runtime-sec5-880319df-amd64.tar

Exported artifact

  /private/tmp/ethereum-lisp-runtime-sec5-880319df-amd64.tar
  sha256 f5745af6372b1dd5f4703beadce22e152d264128c71bded2cdc3c263b594664e

Runtime smoke

cl-workbench validation run runtime-smoke ethereum-lisp-runtime:sec5-880319df-amd64
passed, exit 0 (PASS: runtime image ethereum-lisp-runtime:sec5-880319df-amd64
serves public RPC and an authenticated Engine API).

The live run and the gate reruns at this revision are recorded separately.
